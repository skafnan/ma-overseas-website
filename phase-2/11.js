function getuser(){
    setTimeout(function() {
        const userids =[10,20,30,40]
        console.log(userids)
        setTimeout(function(id){
            const user ={
                name : "shaik afnan"
                age : 23
            };
            console.log('user id: '+id + )
            setTimeout(() => {
                
            }, timeout);
        }
    }
}