// 46. What are pure functions? Why are they important in functional
// programming?

// A pure function is a function that:

// Always returns the same output for the same input

// Has no side effects (does not change external variables, state, or data)

function add(a, b) {
  return a + b;
}